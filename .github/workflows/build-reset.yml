name: Build settings-reset firmware

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@fa33e35f11d2b15311973cda9fb89dcd2376888c
    with:
      build_matrix_path: build-reset.yaml
      archive_name: settings-reset
